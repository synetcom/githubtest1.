// prompt   methodto store user's input
let age = prompt("What's your age?");

// double ampersand for dual contition
// if you have only one sigle line statemnt, you don't need {}
// Curly brackets or Curly braces
if (age >= 18 && age <= 35) {
  console.log("You are good.");
} else {
  console.log("You are not allowed to come in");
}
